import { StyleSheet } from 'react-native';
import type { Theme } from '../../theme';

export function createChavrutaBodyTextStyles(_theme: Theme) {
  return StyleSheet.create({
    gemaraRun: {
      fontWeight: '800',
    },
  });
}
